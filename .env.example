# Use a single data source directory (CSV loader — default: data/sample)
MIDAS_DATA_DIR=data/sample

# Use composite loader across institution subdirectories (real data)
# MIDAS_DATA_ROOT=data

# Plaid (future)
# PLAID_CLIENT_ID=your_client_id_here
# PLAID_SECRET=your_secret_here
# PLAID_ENV=sandbox
