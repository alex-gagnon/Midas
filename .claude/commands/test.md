# /test

Run the full pytest suite and report results.

## Steps

1. Run: `.venv/Scripts/pytest.exe tests/ -v`
2. Report pass/fail counts per file and full details of any failures

## Notes

- Do NOT run git commands
- Do NOT modify implementation files — surface failures for the code executor to fix
